{
  "additionalProperties": false,
  "properties": {
    "type": {
      "type": "string",
      "enum": [
        "columns"
      ]
    },
    "controls": {
      "type": "array",
      "items": {
        "type": "object",
        "minItems": 1,
        "oneOf": [
          { "$ref": "combo.json#" },
          { "$ref": "date.json#" },
          { "$ref": "datetime.json#" },
          { "$ref": "label.json#" },
          { "$ref": "money.json#" },
          { "$ref": "number.json#" },
          { "$ref": "radio.json#" },
          { "$ref": "separator.json#" },
          { "$ref": "text.json#" },
          { "$ref": "textarea.json#" }
        ]
      }
    }
  },
  "required": [
    "type",
    "controls"
  ]
}